{
  "name": "arcane-templates",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "type-check": "tsc --noEmit",
    "generate": "tsx scripts/build-registry.ts",
    "ci": "pnpm run type-check && pnpm run generate",
    "deploy": "wrangler version deploy"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "wrangler": "^4.42.2"
  }
}
